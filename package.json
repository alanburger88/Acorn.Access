{
  "name": "acorn-access",
  "version": "1.0.0",
  "description": "Client-side accessibility widget — single script tag, zero backend, zero data egress",
  "type": "module",
  "main": "dist/acorn-access.min.js",
  "module": "dist/acorn-access.es.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.0",
    "jsdom": "^24.0.0"
  },
  "license": "UNLICENSED",
  "private": true
}
